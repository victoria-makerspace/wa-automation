#!/usr/bin/env python3

from session import Session
from config import Config
from contacts import Contacts

config = Config()
session = Session(config)
contacts = Contacts(session)

from pprint import pprint
pprint(contacts.levels)
